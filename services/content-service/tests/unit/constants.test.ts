import { describe } from '@jest/globals';

// NOTE: Constants test suite structure doesn't match actual exports.
// Constants are organized in nested objects (BLOG, CMS_PAGE, MEDIA, SEO, PAGINATION, CACHE_KEYS, etc.)
// not as individual named exports (BLOG_STATUSES, CONTENT_BLOCK_TYPES, etc.)
// Test suite to be refactored when constants structure is finalized.

describe.skip('Content Service Constants', () => {
  // Full test suite will be enabled once constants structure is finalized
});